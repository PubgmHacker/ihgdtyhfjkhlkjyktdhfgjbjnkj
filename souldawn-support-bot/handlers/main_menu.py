"""SOULDAWN Support Bot — stub.

The support bot has its own /start handler in handlers/support.py.
This file exists to prevent import errors from leftover main-bot code.
"""
from aiogram import Router

router = Router()
# No handlers — /start and all support menu logic live in support.py.